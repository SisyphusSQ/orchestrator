# 执行计划：增加配置热加载

- issue_id:
- 目标：服务无需重启即可重新加载允许热更新的配置；非法配置不能覆盖当前有效配置。

## 范围

包含配置读取、校验、显式 reload 入口及验证；不改配置格式，不增加后台轮询，不处理必须重启的配置项。

## 当前事实与决策

示例文件为 internal/config/loader.go、internal/http/reload.go，实施前替换为真实路径。先验证候选配置，再原子替换，避免保留半更新状态。

## 实现步骤

1. 增加候选配置解析和边界校验。
2. 在经授权的 reload 入口调用 loader，返回可定位错误。
3. 补充成功、非法配置和并发 reload 的验证。

## 验证与验收

| 命令或行为检查 | 预期结果 | 实际结果 |
| --- | --- | --- |
| go test ./internal/config/... | 合法配置替换、非法配置保留原值 | 未执行 |
| go test ./internal/http/... | reload 成功、失败和权限边界符合契约 | 未执行 |

## 失败恢复

校验失败不替换现有配置；重启后仍从原配置入口启动。需要确认项目已有的 reload 权限和并发约束。

## 结果与后续

未开始，下一步为确认真实入口与验收条件。本文件是示例，不能作为已完成证据。
